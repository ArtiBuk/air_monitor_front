services:
  frontend:
    build:
      context: ../..
      dockerfile: docker/frontend/Dockerfile
    container_name: air-monitor-frontend
    hostname: air-monitor-frontend
    restart: unless-stopped
    environment:
      BACKEND_UPSTREAM_HOST: ${BACKEND_UPSTREAM_HOST:-air-monitor-web}
      BACKEND_UPSTREAM_PORT: ${BACKEND_UPSTREAM_PORT:-8000}
    ports:
      - "${FRONTEND_PORT:-80}:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1/healthz || exit 1"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - air_monitor_network

  frontend_exporter:
    image: nginx/nginx-prometheus-exporter:1.5.1
    container_name: air-monitor-frontend-exporter
    hostname: air-monitor-frontend-exporter
    restart: unless-stopped
    command:
      - "--nginx.scrape-uri=http://air-monitor-frontend:8081/stub_status"
    depends_on:
      frontend:
        condition: service_healthy
    networks:
      - air_monitor_network

networks:
  air_monitor_network:
    driver: bridge
